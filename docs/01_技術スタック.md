# ManaBloom 技術スタック

## 1. 文書情報

- 対象バージョン: 0.1.0
- 作成日: 2026-08-12
- 対象: 初期リリース版（端末内保存・課金なし）

## 2. システム概要

ManaBloomは、ユーザー自身が問題を作成して反復学習するレスポンシブPWAである。Android、iOS、PCのWebブラウザを対象とし、ログインやサーバーDBを使用せず、問題・回答履歴・設定を利用端末内に保存する。

## 3. 採用技術

| 区分 | 技術 | バージョン | 用途 |
|---|---|---:|---|
| 言語 | TypeScript | 5.9.3 | FEおよびデータ層の実装 |
| UI | React | 19.2.6 | コンポーネント、状態管理、画面制御 |
| フレームワーク | vinext | 1.0.0-beta.2 | Reactアプリのビルド、SSR、Worker出力 |
| ビルド | Vite | 8.0.13 | 開発サーバー、バンドル |
| 端末内DB | IndexedDB | ブラウザ標準 | 問題、履歴、設定の永続化 |
| DBラッパー | Dexie | 4.2.1 | IndexedDBのスキーマ・検索・トランザクション |
| PWA | Web App Manifest | ブラウザ標準 | ホーム画面への追加、スタンドアロン表示 |
| オフライン | Service Worker / Cache API | ブラウザ標準 | アプリ資材のキャッシュとオフライン起動 |
| 実行環境 | Cloudflare Workers互換ESM | - | 公開時のWeb配信環境 |
| 静的解析 | ESLint | 9.39.4 | コード品質、React、アクセシビリティ検査 |
| 型検査 | TypeScript Compiler | 5.9.3 | 静的型検査 |
| テスト | Node.js Test Runner | Node.js 22以上 | ビルド成果物の基本応答テスト |

## 4. 現在使用していない技術

- C# / ASP.NET Core: 現時点ではサーバーAPIがないため未使用。
- PostgreSQL: データを端末内に保存するため未使用。
- Flutter: PWAとして提供するため未使用。
- 認証基盤: ログイン機能がないため未使用。
- 課金基盤: 初期リリースでは課金機能を無効化し、「リリース検討中」と表示する。

## 5. 技術選定理由

- ReactとTypeScriptにより、既存スキルを活用しつつスマホ・PC共通のUIを一つのコードベースで提供できる。
- PWAにより、Flutterなどの大容量SDKを追加せず、ホーム画面への追加とオフライン利用を実現できる。
- IndexedDBとDexieにより、DBサーバーや月額費用なしで構造化データを永続化できる。
- JSONバックアップにより、サーバーなしでもデータ保全と別端末への手動移行ができる。

## 6. 制約

- データは自動同期されず、ブラウザ・端末・オリジンごとに独立する。
- サイトデータ削除やアプリデータ消去によりデータが失われる可能性がある。
- 端末移行にはJSONバックアップの書き出しと読み込みが必要である。
- 初回アクセスとPWAインストールにはオンライン接続が必要である。
- 公開前にAndroid ChromeおよびiOS Safariで実機確認を行う。

## 7. 将来拡張

ログイン、自動同期、共有、課金を導入する場合は、C# / ASP.NET Core APIとPostgreSQLを追加する。端末内DBはオフラインキャッシュとして残し、同期APIとの競合解決、ユーザー所有権、認証・認可を新たに設計する。
