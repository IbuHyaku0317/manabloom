# コンポーネント構成

ManaBloomの画面は、Atomic Designを参考に次の責務で分けています。

- `atoms`: 戻るボタンなど、これ以上分けにくい最小単位
- `molecules`: 見出しと説明文など、複数の要素をまとめた小さなUI
- `organisms`: ヘッダーやフッターナビなど、画面をまたいで使うまとまり
- `pages`: 各画面固有の表示と操作

`QuizApp.tsx` はデータ取得、画面遷移、クイズ進行の調整役に限定し、表示の詳細は上記コンポーネントへ委譲します。
